import { Router } from 'express';
// import ArticleController from '../../../../controlleurs/k2n service/boutique/controlleurs_market';

const article = Router();

// Route pour créer un nouvel article
// article.post('/articles', ArticleController.createArticle);

// Route pour obtenir tous les articles
// article.get('/articles', ArticleController.getAllArticles);

// Route pour obtenir un article par ID
// article.get('/articles/:id', ArticleController.getArticleById);

// Route pour mettre à jour un article
// article.put('/articles/:id', ArticleController.updateArticle);

// Route pour supprimer un article
// article.delete('/articles/:id', ArticleController.deleteArticle);

export default article;
