<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmation de Recharge</title>
    <style>
        @import url('https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css');
    </style>
</head>
<body class="bg-gray-100 py-10 flex items-center justify-center min-h-screen">
    <div class="max-w-lg bg-white shadow-lg rounded-lg p-6 text-gray-800">
        <!-- Logo -->
        <div class="flex justify-center mb-6">
            <img src="/logo1.png" alt="Logo" class="w-24 h-24">
        </div>

        <!-- Titre -->
        <h2 class="text-xl font-semibold text-blue-400 text-center mb-4">
            Confirmation de votre recharge avec couponguard
        </h2>

        <!-- Contenu principal -->
        <div class="space-y-4">
            <p class="text-gray-700 text-center">
                Bonjour,<br>
                Vous avez effectué une recharge de <strong class="text-blue-500"><%= mount %> <%= devise %></strong>.
            </p>

            <!-- Codes de recharge -->
            <div class="bg-blue-50 border-l-4 border-blue-400 p-4 rounded-md">
                <p class="font-medium text-gray-700">Codes de recharge :</p>
                <p class="text-lg font-mono text-blue-600 mt-2">
                    <%= code1 %> 
                    <% if (code2) { %> - <%= code2 %> <% } %>
                    <% if (code3) { %> - <%= code3 %> <% } %>
                    <% if (code4) { %> - <%= code4 %> <% } %>
                    <% if (code5) { %> - <%= code5 %> <% } %>
                </p>
            </div>

            <!-- Code de transaction -->
            <div class="bg-gray-50 border-l-4 border-gray-300 p-4 rounded-md">
                <p class="font-medium text-gray-700">Code de transaction :</p>
                <p class="text-lg font-bold text-green-600 mt-2"><%= recharge %></p>
            </div>

            <!-- Message administrateur -->
            

            <!-- Message de confiance -->
            <p class="text-gray-500 text-center">
                Merci de nous avoir fait confiance pour le traitement de vos informations.
            </p>
        </div>

        <!-- Bouton retour -->
        <div class="mt-6 text-center">
            <a href="https://coupon-guard.vercel.app/" class="inline-block bg-blue-400 hover:bg-blue-500 text-white font-medium px-6 py-3 rounded-full shadow-md transition duration-300">
                Retour au site
            </a>
        </div>
    </div>
</body>
</html>